from setuptools import setup, find_packages

setup(
    name="dataset_quality_checker",
    version="0.1.0",
    description="A tool for checking data quality in datasets",
    author="Your Name",
    packages=find_packages(),
    install_requires=["pandas"],
)
